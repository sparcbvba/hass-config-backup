name: "GitHub Config Backup"
description: "Synchroniseert specifieke configuratiebestanden en mappen met GitHub"
version: "1.0.1" # Versienummer verhoogd voor de update!
slug: "github_config_backup"
init: false
arch:
  - aarch64
  - amd64
  - armhf
  - armv7
startup: application
boot: auto
map:
  - config:rw
options:
  github_repo: ""
  github_token: ""
  git_name: "Home Assistant"
  git_email: ""
  commit_interval: 3600
  target_paths:
    - "configuration.yaml"
    - "integrations/"
    - "template/"
schema:
  github_repo: str
  github_token: password
  git_name: str
  git_email: str
  commit_interval: int
  target_paths:
    - str